UC7: Computer Makes a Random Move (Easy Level)
Goal

Allow the computer to make a random valid move.

Actor

Computer Player

Flow

Computer turn → random slot generated → converted → validated → placed

Concepts Used
Random Generation
Loop Until Valid
Logic Reuse
Key Requirements
Generate slot between 1–9
Ensure move is valid before placing